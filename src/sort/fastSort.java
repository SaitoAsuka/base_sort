/*
public class fastSort {
    public static void swap(int[] arr, int x, int y) {
        int temp = arr[x];
        arr[x] = arr[y];
        arr[y] = temp;
    }

    public static void fastSort(int[] arr, int point_L, int point_R) {
       if(){

       }
        int point = (int) Math.random() * (point_R - point_L + 1);
        while (point_L <point) {
            if (arr[point_L] > arr[point]) {
                swap(arr, point_L++, point);
            }
        }


    }

    public static void main(String[] args) {
        int[] arr = {3, 7, 0, 4, 6, 5};
        fastSort(arr, 1, 6);
        for (int i = 0; i < 6; i++) {
            System.out.println(arr[i]);
        }
    }
}
*/
